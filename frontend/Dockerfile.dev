# frontend/Dockerfile.dev
FROM node:20-alpine
# Add libc6-compat for lightningcss compatibility on Alpine
RUN apk add --no-cache libc6-compat
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3000
CMD npm install && npm run dev